# Bass Instrument Parameters

```scenario oscilla

note --attach + .$octave

--parameter scale = $scale
--parameter octave = $octave
--parameter tone
--parameter channel = $channel
--parameter distance
--parameter ornaments

```
